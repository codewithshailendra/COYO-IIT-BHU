vector<int> Solution::wave(vector<int> &a) {
    
    sort(a.begin(),a.end());
    
    int n=a.size();
    
    for(int i=0;i<n-1;i++)
    {
        swap(a[i],a[i+1]);
        i++;
    }
    return a;
    
    
    
}
