#include <bits/stdc++.h>
using namespace std;

int main() {
	
string s; cin>>s;
int n=s.length();
 int i=0;
   for(i=0;i<n;i++)
   {
       if(isdigit(s[i]))
          continue;
       else
          break;
   }
	
	
	
   if(i==n)
      cout<<"INTEGER"<<endl;
    else
      cout<<"STRING"<<endl;
	

	return 0;
}
