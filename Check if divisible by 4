#include <bits/stdc++.h>
using namespace std;

int main() {
	
	int t; cin>>t;
	
	while(t--)
	{
	    string s; cin>>s;
	    int n=s.length();
	    int val=(s[n-2]-'0')*10 + (s[n-1]-'0');
	    if(val%4==0)
	       cout<<1<<endl;
	    else
	       cout<<0<<endl;
	}
	
	return 0;
}
